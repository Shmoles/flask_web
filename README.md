# flask_web (Dockerized Flask app)

A minimal Flask web app running inside a Docker container, built by following the Runnable/Wayback “dockerize your Flask application” tutorial and adapting it to the CSCI143 lambda environment (custom port + SSH port forwarding).
